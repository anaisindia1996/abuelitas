<div class="experience-title">
  <h1 class="text-center"><%= @experience.activity_name %> with <%= @experience.grandma_name %></h1>
  <h4 class="text-center pt-3">Hosted by <%= @experience.user.first_name %> <%= @experience.user.last_name %></h4>
  <%# HOSTED BY 'HOST' HERE PLS NIC %>
</div>

<div class="card-product">
  <img src="https://res.cloudinary.com/dg7mx0hnl/image/upload/v1660663917/cdc-F98Mv9O6LfI-unsplash_kqthfz.jpg" />
  <div class="card-product-infos">
    <p><%= @experience.description %></p>
    <p><strong></strong>$<%= @experience.price %></strong></p>
    <p>Available everyday of the week</p>
    <div class="btn text-center mt-4">
      <% if @experience.user == current_user %>
        <p><%= link_to "Delete this experience", @experience, data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></p>
        <%= link_to "Back to All My Experiences", my_experiences_path %>
        <%= link_to "Edit this experience", edit_experience_path %>
      <% else %>
        <%= link_to "Book this experience", new_experience_appointment_path(@experience), class: "text-decoration-none" %>
      <% end %>
    </div>
  </div>
</div>

<div class="map">
  <h2 class="map-title text-center pb-5">This experience takes place at <%= @experience.address %></h2>
  <div style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"></div>
</div>

<div class="bg-white text-center border-top">
  <div class="container pb-5">
  <% if !@experience.reviews.empty?%>
    <h2 class="py-5">Reviews</h2>
  <% end %>
      <div class="list-group">
        <% @experience.reviews.each do |review| %>
          <li class="list-group-item">
            <small>
              <% count = 0 %>
              <% 5.times do %>
                <% if review.rating > count %>
                  <i class="fa-solid fa-star"></i>
                <% else %>
                  <i class="fa-regular fa-star"></i>
                <% end %>
                  <% count += 1 %>
              <% end %>
            </small>
            <p>
              <%= review.content %><br>
              - <%= review.user.first_name %> <%= review.user.last_name %>
            </p>
          </li>
        <% end %>
      </div>
  </div>
</div>
